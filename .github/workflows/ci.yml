name: mori
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build:
    name: mori build
    runs-on: label-1
    container:
      image: rocm/pytorch-private:mori_rocm7.2_ubuntu22.04_py3.10_pytorch_bnxt_ionic
      credentials:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build and install mori
        run: |
          BUILD_EXAMPLES=ON pip install .
      - name: Verify installation
        run: |
          python -c "import mori; print('OK')"
      # - name: Copy build artifacts to node-2
      #   run: |
      #     scp -P 2233 -r $GITHUB_WORKSPACE root@smci355-ccs-aus-n08-33:/root/actions-runner/_work/mori/mori/
  test:
    name: mori test
    needs: build
    runs-on: label-1
    container:
      image: rocm/pytorch-private:mori_rocm7.2_ubuntu22.04_py3.10_pytorch_bnxt_ionic
      credentials:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    timeout-minutes: 30
    env:
      PYTHONPATH: ${{ github.workspace }}:$PYTHONPATH
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install mori
        run: |
          cd $GITHUB_WORKSPACE
          BUILD_EXAMPLES=ON pip install .
      - name: MORI-EP
        run: |
          cd $GITHUB_WORKSPACE
          timeout 120 pytest tests/python/ops/test_dispatch_combine.py -q
      - name: MORI-EP bench
        run: |
          cd $GITHUB_WORKSPACE
          timeout 120 python3 tests/python/ops/bench_dispatch_combine.py
      - name: MORI-IO
        run: |
          cd $GITHUB_WORKSPACE
          timeout 60 pytest tests/python/io/
      - name: MORI-IR
        run: |
          cd $GITHUB_WORKSPACE
          timeout 60 torchrun --nproc_per_node=2 examples/shmem/ir/test_triton_shmem.py
          timeout 60 torchrun --nproc_per_node=8 examples/shmem/ir/test_triton_allreduce.py
      - name: MORI-CPP shmem (P2P)
        run: |
          cd $GITHUB_WORKSPACE
          timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_thread
          timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_get_thread
          # timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_imm_thread
          timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_signal_thread
          timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/atomic_nonfetch_thread
          timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/atomic_fetch_thread
      - name: MORI-CPP shmem (IBGDA)
        run: |
          cd $GITHUB_WORKSPACE
          MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_thread
          MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_get_thread
          # MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_imm_thread
          MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/concurrent_put_signal_thread
          MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/atomic_nonfetch_thread
          MORI_DISABLE_P2P=ON timeout 60 mpirun --allow-run-as-root -np 2 ./build/examples/atomic_fetch_thread
      - name: MORI-CCL/shmem
        run: |
          cd $GITHUB_WORKSPACE
          timeout 600 pytest tests/python/shmem/test_api.py -v
      # inter-node tests: to be handled later
      # - name: MORI-EP inter bench
      #   run: |
      #     cd $GITHUB_WORKSPACE
      #     ssh -p 2233 root@smci355-ccs-aus-n08-33 "GLOO_SOCKET_IFNAME=enp81s0f1 /opt/venv/bin/torchrun --nnodes=2 --node_rank=0 --nproc_per_node=1 --master_addr='smci355-ccs-aus-n08-33' --master_port=1234 $GITHUB_WORKSPACE/examples/ops/dispatch_combine/test_dispatch_combine_internode.py --cmd bench --kernel-type v1 --num-qp 2 --max-tokens 4096 &" &
      #     GLOO_SOCKET_IFNAME=enp81s0f1 torchrun --nnodes=2 --node_rank=1 --nproc_per_node=1 --master_addr="smci355-ccs-aus-n08-33" --master_port=1234 examples/ops/dispatch_combine/test_dispatch_combine_internode.py --cmd bench --kernel-type v1 --num-qp 2 --max-tokens 4096
      # - name: MORI-EP inter stress
      #   run: |
      #     cd $GITHUB_WORKSPACE
      #     ssh -p 2233 root@smci355-ccs-aus-n08-33 "GLOO_SOCKET_IFNAME=enp81s0f1 /opt/venv/bin/torchrun --nnodes=2 --node_rank=0 --nproc_per_node=1 --master_addr='smci355-ccs-aus-n08-33' --master_port=1234 $GITHUB_WORKSPACE/examples/ops/dispatch_combine/test_dispatch_combine_internode.py --cmd stress --kernel-type v1 --num-qp 2 --max-tokens 128 &" &
      #     GLOO_SOCKET_IFNAME=enp81s0f1 torchrun --nnodes=2 --node_rank=1 --nproc_per_node=1 --master_addr="smci355-ccs-aus-n08-33" --master_port=1234 examples/ops/dispatch_combine/test_dispatch_combine_internode.py --cmd stress --kernel-type v1 --num-qp 2 --max-tokens 128
