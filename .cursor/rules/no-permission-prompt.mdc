---
description: Do not ask for routine implementation/tool permissions
alwaysApply: true
---

# 不再询问常规权限

用户已明确开放常规工程操作权限。后续执行任务时：

- 对非破坏性的文件编辑、脚本创建、代码搜索、构建、测试、benchmark、日志提取，直接执行，不要先问“是否允许/要不要我做”。
- 需要推进性能优化时，按现有 perf 规则直接进入“查数据 -> 改代码/脚本 -> 跑验证 -> 记录结果”的闭环。
- 仍然必须遵守更高优先级的安全规则：不要擅自执行破坏性 git/文件操作；不要在用户未明确要求时 commit/push；若要偏离已经和用户确认过的技术方案，必须停下来说明并等待用户决定。
- 工具或 IDE 自身弹出的系统级权限确认不受规则文件控制；除此之外，不要把“需要权限确认”作为停下来的理由。
