---
description: 性能优化用严格时间数据打点验证每步假设，不达目标不停止
alwaysApply: true
---

# 性能优化：实测驱动 + 目标导向

## 触发条件

任何性能优化任务（延迟、吞吐、wall time、overlap 效率、能耗等）。

## 硬性规则

1. **每次尝试都要有 before/after 实测数据对比**。推理、类比、文档、同行经验都不能替代实测。
2. **每次尝试后必须彻底分析**：
   - 实测 vs 预期的差距是多少
   - 差距的根因（若不明，再加 timestamp / instrumentation / profiler 定位到具体 phase）
   - 下一步具体方案（基于刚定位到的根因，不是拍脑袋）
3. **不得用"承认现状"、"架构 inherent limitation"、"物理上做不到"类措辞提前妥协**。
   - 目标未达成前永远假设还有没发现的优化点。
   - 如果真碰壁，也要先列出至少 3 个未验证的候选方向，分别给出验证方式。
4. **用户给出的性能目标是硬指标**。
   - 例如"SDMA copy 超过 RCCL" ⇒ 在 RCCL 被超过之前不提"接受现状"。
   - 每次实验后要量化"距离目标的 gap"，让用户看到进度。
5. **失败的尝试立刻 revert**。主分支（或工作分支的 HEAD）始终维持在"当前可用 baseline"状态，不留已验证失败的代码。
6. **多轮失败时要反思方法论**，而不是只反思单个代码改动。
   - 是否定位到了真正根因？还是在猜测层面修？
   - 用的测量方法是否能反映真实行为？

## 工作流模板

```
while 距目标 gap > 0:
    (1) 分析当前 wall breakdown，找最大 gap 所在 phase
    (2) 列出 3-5 个该 phase 的可能机制假设
    (3) 选风险低、收益高的一个实现
    (4) 实测 before/after
    if 达标:            → 收工
    if 部分改善但未达标: → 保留改动，回 (1) 找下一大 gap
    if 倒退或无变化:     → 立刻 revert，从数据否定该假设，回 (2)
```

## 反模式

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| "架构决定了这是下限" | 列 3-5 个可能的突破方向 + 实测 |
| "CU blit 就是这么慢" | 用 rocprof / HIP trace 确认 copy 走 CU 还是 SDMA |
| "user workload 不重复，方案不适用" | 做 fallback 版本：命中赢，未命中持平（no-regression） |
| 一次测试无效果就放弃方向 | revert 具体改动，但保留方向分析，换实现方式再试 |
| "已经尝试 N 次，接受现状" | 列清尝试过的、未尝试的候选，继续迭代 |

## 量化进度的模板（每次回答用）

- 目标：<用户给的目标，例如 SDMA copy wall < RCCL wall>
- 当前 baseline 实测：<数字>
- 本次尝试实测：<数字>
- 距目标 gap：<数字 + 百分比>
- 下一步假设：<具体机制 + 验证方式>
