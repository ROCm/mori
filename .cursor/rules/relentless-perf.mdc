---
description: 性能优化必须先打点再改代码，用严格的时间数据验证每步假设，不达目标不停止
alwaysApply: true
---

# 性能优化：先打点 → 再改 → 再对比 → 不达目标不停止

## 触发条件

任何性能优化任务（延迟、吞吐、wall time、overlap 效率、能耗等）。

## 最硬性的前置规则（每次动手前必须满足）

### R0. 先有打点，后改代码
在改任何**可能影响 perf 的代码**之前，必须先满足：
- 目标 phase / 目标新操作 **已经有 timestamp / HIP event / host chrono 打点**，或
- 本次 commit 的**前置 commit** 就是"加 instrumentation"。

违反方式举例（本会话发生过）：
- ❌ 先加了 `hipStreamWaitValue32 + hipMemcpy2DAsync` 改动，push，然后被用户问起才补 instrumentation
- ❌ 先换了 copy kernel 实现，然后才测它比原来慢
正确做法：
- ✅ commit N:   加 per-phase instrumentation（host us + gpu ms 每段）
- ✅ commit N+1：基于 baseline instrumentation 看到的瓶颈，做一处改动
- ✅ commit N+2：实测对比，保留/revert

**如果当前分支连打点都没有，下一步永远是"加打点"，不是"改代码"**。

### R1. 每次尝试必须有 before/after 实测数据对比
推理、类比、文档、同行经验、commit message 自我声明都不能替代实测数字。

### R2. 每次尝试后必须彻底分析
- 实测 vs 预期的差距是多少（量化）
- 差距的根因（若不明，再加更细的 instrumentation / profiler 定位到具体 phase / 具体 chunk / 具体 CU）
- 下一步具体方案（基于刚定位到的根因，不是拍脑袋）
- 若改动使 wall 变差或无变化，**立刻 revert**

### R3. 不得用"承认现状"类措辞提前妥协
禁止措辞（任何一条出现 = 违规，重写回复）：
- ❌ "架构 inherent limitation" / "物理上做不到" / "架构极限附近"
- ❌ "SDMA 天生比 CU blit 慢"
- ❌ "已经尝试 N 次，接受现状"
- ❌ "user workload 不重复，方案不适用"
- ❌ "要不要收工"/"你决定继续挖 ... 还是收工" ← **把"收工"作为 option 递给用户本身就是提前妥协**
- ❌ "承认架构极限/噪声级/接近物理下限" —— 这类话**不得用来建议停止**

若真碰壁，也要先列出至少 **3 个未验证的候选方向** + 每个候选的**验证方式**，再继续工作。

### R4. 用户给出的性能目标是硬指标 —— 问题必须解决才停止
- "SDMA copy wall < RCCL wall" 是**必达项**，不是"尽力而为"
- **目标未达成前不得建议用户停止或收工**，包括但不限于：
  - 不得在回复末尾给"收工"选项
  - 不得用"或者承认现状"作为 fallback
  - 不得以"gain 不够"为由停止 —— 找更大 gain 方向
- 每次实验后量化"距离目标的 gap"，让用户看到进度
- 数字退步也要报，不得只报好看的
- **若当前方向 gain 不够，必须继续列新方向（至少 3 个，按 R3），而不是问用户是否停下**

### R5. 失败尝试立刻 revert
主分支或工作分支的 HEAD 始终维持在"当前可用 baseline"状态，不留已验证失败的代码。Revert 后在回复里简述：为什么失败（数据）、否定了哪个假设、下一步换哪个方向。

### R6. 多轮失败时反思方法论而不是只反思代码
- 是否定位到了真正根因？还是在猜测层面修？
- 用的测量方法是否能反映真实行为？（e.g. `hipMemcpyAsync` 真走 SDMA 还是 CU blit？有没有 trace 验证？）
- 是否还有根本性机制（内存访问模式 / 调度方式 / 内存拓扑）没看过？

### R7. 性能对比只看"与当前主 API 同语义"的路径

当前 SDMA AllReduce 的主 API = `copy_output_to_user=True` 路径：用户传自己的
output tensor，AR 调用返回后**直接从该 output tensor 读结果**。这条路径才是对标
RCCL `dist.all_reduce(tensor)` 的 drop-in 替代语义。

**禁止**把下列路径拿来当"已超 RCCL"的证据或收工理由：

- ❌ `copy_output_to_user=False` + `get_output_transit_buffer()` 读结果
  - 理由：该 API 要求用户框架层感知 transit buffer、手动替换 tensor 来源、
    处理 ownership / 生命周期；**不是 RCCL 的 drop-in replacement**，违反
    "对标 RCCL 使用方式"的前提。
- ❌ "no-copy 模式 wall 7.xxx ms 已经超 RCCL" 类陈述
  - 理由：该模式用户 output tensor 未被写入，结果不在用户 tensor 里，不是可用 API 形态。
- ❌ 任何要求调用方代码修改 / 替换 tensor 指针 / 额外 sync / 额外 register 步骤的
  "优化"方案都不算达标。

**只接受**下列作为"目标已达成"的证据：

- ✅ 默认调用 `AllreduceSdma(copy_output_to_user=True)(input, output, ...)` 后，
  output tensor 立刻可用，且 multi-stage overlap wall < RCCL wall
- ✅ 内部实现可以任意改（double-buffer / 独立 stream / kernel 改写 / lazy shmem
  register 用户 output 等），**但对用户调用方必须零感知、零代码改动**。

**任何"规避"这条规则的措辞（"只需要用户稍微改一下 API 调用"、"这个开销 user
也能接受"）都视为违规，必须重新读这条规则**。

### R8. 收益大的方案先验证，数据说话

当存在多个可选方向时，必须按"**预期 gain 从大到小**"排序，**不得**以"风险低"、"改动小"、
"更熟悉"、"更保险"为由跳过高收益方向去先做低收益方向。

**硬性流程**：

1. **枚举候选方向**，每个给出：
   - 机制（为什么这个方向能产生 gain）
   - 预期 gain（ms / %，用当前实测数据推算，必须标"实测推算"或"纯猜"）
   - 实现复杂度
   - 已知风险
2. **按预期 gain 排序**，大的在前。并排到排第 1 的方向：
   - 如果预期 gain < 目标 gap × 1.5（留 margin），**必须**说明为什么还要做
   - 如果预期 gain < 目标 gap × 0.5（明显不够吃），**不得**作为主攻方向
3. **主攻方向必须先补数据打点验证假设**（R0）：
   - 列出"这个方向能 work"的**关键假设**（1-3 条）
   - 为每条假设设计一个**最低成本实测验证**
   - 验证通过再进入实现；验证失败换下一个方向
4. **排除路径也要记录**：每个被排除的方向写清"因为 <数据/机制> 所以排除"，
   不是"感觉不行"。

**反模式**：

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| "B 风险低我先做 B" | "B 预期 gain 0.1 ms，E 预期 gain 0.3 ms，目标 gap 0.2 ms → 先做 E" |
| "我先做个简单的试试" | "先做收益最大的 E；E 的关键假设是 <X>，用 <实测方式> 验证 X" |
| 把多个方向平行推进 | 一次只主攻一个方向，做完拿到数据再转向 |
| 不给出每个候选的预期 gain | 每个候选必须有数字（即使是粗估），否则排序失效 |
| 在**小 gain 方向**消耗多个 commit 来回折腾 | 若 gain 预期已小于 gap，直接跳过，不浪费精力 |

**本会话的违规示例**：先花精力在方向 B（double buffer copy stream，预期 gain 0.1 ms）
而 gap 是 0.2 ms —— gain < gap，按 R8 应立即跳过 B 直接上 gain ≥ gap 的方向。
**现在开始必须按 R8 执行**。

### R9. 查询并记录历史测试中有价值的时间数据

每次有价值的实测数据（wall / phase / gain / loss / side-effect）**必须沉淀到 repo
内的 perf_history.md**（推荐路径 `docs/perf_history.md`），至少记录：

- **日期 + HEAD SHA**（或 commit 范围）
- **场景**：test 命令行 + 关键 env var（`MORI_PIPELINE_CU`、`MORI_POST_AG_WAIT`、`MORI_PHASE_TARGET_STAGE` 等）、size、stages、iter、warmup
- **指标 + 数字**：wall（copy / no-copy / RCCL）；per-stage AR dur；AR[0] Phase breakdown；copy host us / gpu ms；register us 等
- **机制解释**：gain 来自哪个机制 / loss 来自 X / side-effect 分析
- **结论**：验证通过 / 验证失败（具体 kill-switch 触发）/ 归入哪个方向的基线

**每次新优化方案设计前，必须先查 `perf_history.md`**：

- 已经 fail 的方向（有数据否定过的）不得再走，除非有新机制解释为何此次会不同
- 已知瓶颈数字（各 phase ms 上限 / 下限）作为**预期 gain 估算的依据**（R8 的 gain 推算不得凭空猜）
- 找可以**复用已有数据**的地方，不重复跑耗时实验

**每次完成 perf 相关的 commit 时**：

- commit message 必须指向 `perf_history.md` 的**新增条目**（例如 "see perf_history.md §entry-N"）
- `perf_history.md` 的新条目必须包含**该 commit 的 SHA**（形成双向链接）
- 若 revert 某个失败尝试，`perf_history.md` 的条目里标注 "REVERTED at <SHA>" 并简述原因

**反模式**：

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| 本次估算 gain 靠"我记得之前测过"，没查 | 先 grep `perf_history.md` 找到具体数字再引用 |
| 跑新实验前没看过已有数据 | 先读 ledger 最新 3-5 条，看瓶颈定位 |
| 实测完数字只贴在对话里 | 立即 commit 到 ledger，再回复用户 |
| "这个方向之前试过失败" —— 但找不到具体原因 | ledger 里查具体失败 commit + 机制 |

### R10. 历史数据作为优化方案的必要输入

每个新方案的提案/回复中必须**至少引用 2 条历史数据**作为依据：
- 1 条支持 "这个方向能 work" 的机制论据
- 1 条支持 "预期 gain 数字" 的量化依据

若引用不出 2 条数据，说明数据不足，**先补打点**，不强行推进方案。

### R11. 批量对比实验 = 单一可复现 shell block（跑测 + 提取 + 打印对比 一次完成）

**触发**：一次给用户 ≥ 2 条对比命令（例如 baseline vs 方案 A vs 方案 B vs 组合）。

**硬性要求 —— 用户只 copy-paste 一次就能得到对比结果**：

1. **跑测 + 提取 + 打印对比表在同一个 shell block 内**（单次 copy-paste 完成）：
   ```bash
   set -euo pipefail              # fail-fast: 任一步 error 立刻 abort
   cd <repo> && git pull ...      # rebuild 前置
   pip install -e . --no-build-isolation   # NO | tail -N ！失败要让 exit code 暴露
   LOG=/tmp/perf_<topic>_$(date +%s).log
   { ...循环跑所有变体... } | tee "$LOG"
   # 紧接着（同一 block 内）：
   awk/grep 从 $LOG 提取并直接打印对比表（用户无需再粘一次命令）
   echo "LOG: $LOG"
   ```
2. **必须 `set -euo pipefail` 开头**。否则 `pip install ... | tail -3` 在 pip
   失败时 tail 仍 exit 0，`&&` 放行后续测试命令，白跑几分钟得到空输出。
3. **`pip install` 不得带 `| tail -N`**（会吞掉 exit code）。让它的 stdout/stderr
   直接输出到终端（tee 到 $LOG），失败时立刻停止；日志里自带完整 build error。
4. **所有变体输出**重定向到**同一个 `$LOG` 文件**（`tee "$LOG"`），
   每段前用 `========== <LABEL> ==========` 分隔，便于 `awk` 按 label 切段。
5. **log 文件名含时间戳**（`/tmp/perf_<topic>_$(date +%s).log`），避免覆盖。
6. **block 末尾 `echo "LOG: $LOG"`**，用户贴回结果时我能直接定位。
7. **提取逻辑内嵌在同一 block**，至少自动打印：
   - 各 label 的关键指标对比（wall ms / phase ms / gap vs RCCL）
   - per-phase breakdown 里 gap 最大的 phase
   - 机制假设对应的 side-metric（例如 GEMM dur、AG-submit→AG-wait-done）
8. **用户只需要贴回 1 段输出**（stdout 的全部内容，含对比表）。
   不得要求用户"然后再跑下面这条提取命令"。

**反模式（必须避免）**：

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| 给用户 4 条独立 `python test ... \| grep wall` 命令，让用户逐条跑 | 1 个 bash for 循环跑 4 变体，tee 到 $LOG |
| "然后请再跑下面这条 awk/grep 从 $LOG 提取" | awk/grep 内嵌在同一 block，紧跟 tee 之后 |
| 只给测试命令，让用户 scroll 几千行 log 找数字 | block 末尾直接打印 ready-to-read 的对比表 |
| 让用户手工把 4 段 stdout 拼到一起贴回来 | 一次 `tee "$LOG"` + 一次提取 = 一次粘贴 |
| 多次 pip install / source env 散落在各 label 之间 | 1 次 pip install + 1 次 git pull 放在 block 开头 |
| `pip install ... 2>&1 \| tail -3` 后用 `&&` 串联测试 | 不 pipe tail；用 `set -euo pipefail` 保 exit-code 生效 |
| 用户贴回"pip build failed"后继续输出空 label，我没看出 | `set -e` 后第一条 fail 立刻 abort，不白跑 |

**本会话发生过的违规**：
- R11 v1：给"跑"+"提取"两个 block，要求用户贴两次 → v2 已合并为一个 block。
- R11 v2：`pip install ... | tail -3 && <后续>` → tail 吞掉 exit code，build
  failed 还继续跑测试，用户看到假的"空 OFF 段"。**必须用 `set -euo pipefail`**。
- R11 v3：pip install 之前没做 toolchain preflight（cmake/hip/gpu 可用性），
  build 直接因 `cmake not found` 失败，用户浪费一轮运行 —— 按 R12 补前置检查。

### R12. 给出 shell block 之前必须做**环境 preflight**

**触发**：任何需要用户在远端机器/容器运行的 shell block（尤其涉及 compile、
HIP、multi-gpu、MPI、特殊驱动 / sudo 等）。

**硬性要求**：shell block 的**第一段**必须是 preflight，失败即退（不跑后面）：

```bash
set -euo pipefail

# 0. 当前在哪台机 / 容器内外 / 是不是 root
hostname; id -un; pwd

# 1. 编译工具链（按本任务所需）
command -v cmake  >/dev/null || { echo "MISSING: cmake";  exit 1; }
command -v hipcc  >/dev/null || { echo "MISSING: hipcc";  exit 1; }
command -v python3>/dev/null || { echo "MISSING: python3";exit 1; }
cmake --version | head -1
hipcc --version 2>/dev/null | head -2 || true

# 2. 运行时 —— GPU / HIP 可用
rocm-smi --showproductname 2>&1 | head -5
python3 -c "import torch; \
  assert torch.cuda.is_available(), 'HIP not available'; \
  print('devices:', torch.cuda.device_count())"

# 3. 代码状态：当前 SHA、是否 clean、是否 on 预期 branch
git -C "$REPO" rev-parse --abbrev-ref HEAD
git -C "$REPO" log -1 --oneline
git -C "$REPO" status --short || true
```

**不得**：
- ❌ 直接 `pip install -e .` 然后指望 cmake / hipcc / torch.cuda 都 ready
- ❌ 把环境假设隐式写在注释里（"请先进容器"），不 preflight 就往下跑
- ❌ preflight 失败时继续往下（所以必须 `set -e`，不能用 `||true` 吞掉）

**已知的易错项**（本会话踩过的）：
- 用户在跳板机 `user@host:~$` 而非容器 `root@host:/path#` → preflight 输出
  `hostname`/`id` 能立即暴露
- 容器里 `cmake` / `hipcc` 缺失 → preflight 输出 `MISSING:` 并 exit 1
- `ulimit -l` 不 unlimited 导致 HIP 初始化失败 → preflight 输出 `torch.cuda.device_count()=0`
  立即退，不让用户错以为是代码 bug
- 残留 zombie 进程占 GPU → `rocm-smi --showpids` 可以加到 preflight 里报警

**反模式**：
| ❌ 错误 | ✅ 正确 |
|--------|--------|
| "请确保你在容器内运行" 写在注释 | `hostname; id -un` 作为 preflight 第一行 |
| pip install 失败了我才问"你装 cmake 了吗" | preflight 已经检查 cmake |
| 空输出 OFF 段才发现 pip install 失败 | preflight 在 pip 之前 exit 1 了 |
| 跨轮对话默认用户环境没变 | 每个 shell block 都自带 preflight，不靠记忆

### R13. `set -e` / `exit` 必须隔离在 subshell / here-doc，不得污染用户的交互 session

**背景事故（本会话）**：R11 v3 教用户在交互 shell 顶层 `set -euo pipefail`，
然后跟一串 `command -v cmake || exit 1` / `command -v hipcc || exit 1`。
hipcc 找不到 → `exit 1` 直接把用户**登录的 bash session 杀了**；在容器里等价于
**退出容器**。用户还以为是我命令写错。

**硬性要求**：凡是给用户粘贴的 "preflight + 多步跑" 类 shell block，
必须满足以下**至少一种**隔离方式：

1. **Here-doc 到 `bash`（首选）**：
   ```bash
   bash <<'RUN_ALL'
   set -euo pipefail
   ... preflight ...
   ... build ...
   ... run + tee ...
   ... awk/grep 打印对比表 ...
   RUN_ALL
   ```
   优点：单次 copy-paste 不破坏用户 session；bash 子进程失败只影响它自己。

2. **命令组 `(...)` subshell**：
   ```bash
   (
     set -euo pipefail
     ... 全部步骤 ...
   )
   ```
   注意：**不要**写成 `{ ...; }` 复合命令（那是当前 shell，`exit` 会退 session）。

3. **写成 `.sh` 文件后 `bash script.sh`**：
   适合步骤 > 20 行的大脚本，可复用。

**禁止**：
- ❌ 在**粘贴给用户的顶层**直接写 `set -e` / `exit 1`（会杀用户 shell）
- ❌ 用 `{ ...; }` 冒充 subshell（**不是** subshell）
- ❌ `|| exit 1` 在用户交互 shell 里出现 —— 必须 `|| { echo ...; return 1; }`
  放在函数 / subshell 内部

**反模式**：
| ❌ 错误 | ✅ 正确 |
|--------|--------|
| `set -e; command -v cmake \|\| exit 1` 贴到用户 shell 顶层 | `bash <<'EOF' … EOF` 包起来 |
| `{ set -e; pip install...; }` | `( set -e; pip install...; )` 或 here-doc |
| "如果哪步挂了，重开终端"（已经晚了） | 一开始就 subshell 隔离，不挂 session |

**self-check before sending shell block**：
- [ ] 我这段脚本里是否有 `set -e` / `exit N`？
- [ ] 如果有，顶层是否已被 `bash <<'EOF' ... EOF` 或 `( ... )` 包起来？
- [ ] 如果 preflight 的某个 `command -v` 失败，用户的 shell/容器会不会退？

## 工作流模板（每轮都走这个）

```
while 距目标 gap > 0:
    0)  查 perf_history.md（R9）：已知瓶颈、已 fail 方向、可复用数据
    1)  相关 phase 有没有打点？没有 → 先加打点 → commit → 跑 baseline → 记录数据
    2)  数据分析当前 wall breakdown，找最大 gap 所在 phase
    3)  列出 3-5 个该 phase 的可能机制假设，每个估算预期 gain (R8)
        每个方向引用 ≥2 条历史数据作为依据 (R10)
    4)  按预期 gain 排序，选 gain 最大且 gain ≥ gap × 1.5 的方向（R8）
        若所有方向 gain < gap：先找 gain 更大的新方向，不急着动手
    5)  实施前必须先打点验证该方向的关键假设（R0）
    6)  实测 before/after（必须用打点数据，不得用"肉眼/直觉"）
    7)  实测结果写入 perf_history.md（R9），双向 link commit SHA
    if 达标:                → 收工，总结根因
    if 部分改善但未达标:    → 保留改动，回 (1) 找下一大 gap
    if 倒退或无变化:        → 立刻 revert，从数据否定该假设，回 (2)
```

## 每次回答必须给用户看到的内容

**量化进度模板**：
- 目标：<用户给的目标，例如 SDMA copy wall < RCCL wall>
- 当前 baseline 实测：<数字 + 从哪条命令/log>
- 本次尝试实测：<数字 + 从哪条命令/log>
- 距目标 gap：<数字 + 百分比>
- 本次改动对应的打点位置：<文件 + 函数 + 打点 label>
- 下一步假设：<具体机制 + 验证方式 + 预期数字>

若上述任何一项写不出来，**不得发出回复**，要么先去补数据，要么明确告诉用户"我需要先加打点/跑基准，不能空谈"。

## 反模式

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| 没打点直接改代码 | 先 commit instrumentation，跑完 baseline 再改 |
| "推理上应该 X 更快" | "实测 X=Ams，Y=Bms，X 快 (A-B)ms = N%" |
| "架构决定了这是下限" | 列 3-5 个可能的突破方向 + 实测验证每一个 |
| "CU blit 就是这么慢" | 用 rocprof / HIP trace 确认 copy 走 CU 还是 SDMA，测 HBM 带宽利用率 |
| "user workload 不重复，方案不适用" | 做 fallback：命中赢，未命中持平（no-regression） |
| 一次测试无效果就放弃方向 | revert 具体改动，保留方向分析，换实现方式再试 |
| "已经尝试 N 次，接受现状" | 列清尝试过的 + 未尝试的候选，继续迭代 |
| 只贴好看的数字 | 退步也贴，gap 计算必须公开 |
| 规则写了但自己不遵守 | 每次回答前自检 R0~R6 是否都满足 |
